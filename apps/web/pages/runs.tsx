export default function RunsPage() {
  return (
    <div>
      <h2 style={{ marginBottom: "1rem" }}>Runs</h2>
      <p style={{ color: "var(--text-muted)" }}>
        Application runs will appear here once Playwright auto-apply is
        implemented (Phase 3).
      </p>
    </div>
  );
}
